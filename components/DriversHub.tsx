// Fix: Replaced invalid content with a valid React component.
import React from 'react';

const DriversHub: React.FC = () => {
  return (
    <div>
      <h2>Drivers Hub</h2>
      <p>View driver stats and standings.</p>
    </div>
  );
};

export default DriversHub;
